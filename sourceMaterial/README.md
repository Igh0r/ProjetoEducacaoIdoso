
  # App idoso

  This is a code bundle for App idoso. The original project is available at https://www.figma.com/design/NQaeSfw7rI9waVONnMealy/App-idoso.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  